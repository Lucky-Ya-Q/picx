const PREFIX = 'PICX_'

/**
 * LocalStorage Key
 */
export const LS_CONFIG = `${PREFIX}CONFIG`
export const LS_MANAGEMENT = `${PREFIX}MANAGEMENT`
export const LS_SETTINGS = `${PREFIX}SETTINGS`

/**
 * SessionStorage Key
 */
export const SS_GLOBAL_SETTINGS = `${PREFIX}GLOBAL_SETTINGS`
